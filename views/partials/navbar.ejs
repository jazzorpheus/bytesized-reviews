<nav class="navbar navbar-expand-md sticky-top" id="navbar">
  <div class="navbar-container container-fluid">
    <a class="navbar-brand" href="/">Bytesized Book Reviews</a>
    <button
      class="navbar-toggler navbar-dark"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarNavAltMarkup"
      aria-controls="navbarNavAltMarkup"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon navbar-dark"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav ms-auto d-flex align-items-start">
        <a class="nav-link" href="/">Home</a>
        <a
          class="nav-link <%= url === '/reviews' ? 'active' : '' %>"
          href="/reviews"
          >All Reviews</a
        >
        <a
          class="nav-link <%= url === '/reviews/new' ? 'active' : '' %>"
          href="/reviews/new"
        >
          <i class="fa-solid fa-plus"></i> New Review
        </a>
        <% if (!currentUser) { %>
        <a
          class="nav-link <%= url === '/login' ? 'active' : '' %>"
          href="/login"
          >Log In</a
        >
        <a
          class="nav-link <%= url === '/register' ? 'active' : '' %>"
          href="/register"
          >Register</a
        >
        <% } else { %>
        <a class="nav-link" href="/logout">Log Out</a>
        <% } %>
      </div>
    </div>
  </div>
</nav>
